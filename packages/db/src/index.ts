export type { RegistryRepository } from "./registry.repository.js";
export type { UserDbRepository } from "./user-db.repository.js";

// MesaHub implementations will be added here once the API spec is available.
// Cloud repo can also inject its own implementations via the repository pattern.
